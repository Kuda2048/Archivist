{
  "name": "archive-reader",
  "version": "1.1-alpha-20260723",
  "description": "Local-first reader for AI chat exports (Claude, ChatGPT)",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor-community/sqlite": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
